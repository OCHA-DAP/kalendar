[build-system]
requires = ["setuptools>=45", "setuptools_scm[toml]>=6.2"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "src/kalendar/_version.py"
local_scheme = "no-local-version"
version_scheme = "release-branch-semver"
